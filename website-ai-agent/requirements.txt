# Runtime dependencies (mirror of pyproject.toml [project.dependencies]).
# Kept for users who install without pip's pyproject support; pyproject.toml is the source of truth.
pydantic>=2.7
pydantic-settings>=2.3
rich>=13.7
playwright>=1.45
openai>=1.40
langgraph>=0.2
